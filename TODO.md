- [ ] repel support
- [ ] random name on startup
- [ ] hgsdk update
- [ ] fighting + pc
    ```
    player is in search mode:
    - if MATCHES beep
    - if NOT MATCHES run
    player is in fight mode:
    - idle check for:
      - if pp low (10%) use pc path
      - if hp low (20%) / dead run away then use pc path
    - if MATCHES beep
    - if NOT MATCHES fight:
    ```
- [ ] paths + displaying on the actual map
- [ ] rename controls to match functionality

less priority/maybe impossible:

- iv/nature read
- Future position predictor
- some pokemon can lock you in battle and you have to kill them
- catch pokemon function
